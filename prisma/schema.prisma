// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}


// テスト接続用
model TestConnection {
  id    Int     @id @default(autoincrement())
  @@map("test_connection")
}


// ログインマスタ
model FrontUserLoginMaster {
  userId      Int   @id @map("user_id")
  password    String   @db.VarChar(255)  @map("password")
  createDate  DateTime  @map("create_date")
  updateDate  DateTime  @map("update_date")
  deleteFlg   String   @db.Char(1)  @map("delete_flg")
  @@map("front_user_login_master")
}


// フロントユーザーマスタ
model FrontUserInfoMaster {
  userId      Int   @id @map("user_id")
  userName    String   @db.VarChar(255)  @map("user_name")
  createDate  DateTime  @map("create_date")
  updateDate  DateTime  @map("update_date")
  deleteFlg   String   @db.Char(1)  @map("delete_flg")
  @@map("front_user_info_master")
}


// お気に入り動画
model FavariteMovieTransaction {
  userId      Int   @id @map("user_id")
  movieId    String   @db.VarChar(255)  @map("movie_id")
  createDate  DateTime  @map("create_date")
  updateDate  DateTime  @map("update_date")
  deleteFlg   String   @db.Char(1)  @map("delete_flg")
  @@map("favarite_movie_transaction")
}


// シーケンスマスタ
model SeqMaster { 
  key         String @id @db.VarChar(1000)
  nextId      Int
  createDate  DateTime @map("create_date")
  updateDate  DateTime @map("update_date")

  @@map("seq_master") 
}